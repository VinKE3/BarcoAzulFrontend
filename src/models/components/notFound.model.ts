export interface INotFound {
  full?: boolean; // Propiedad opcional para determinar si la vista debe ocupar toda la pantalla
  notFound?: boolean; // Propiedad opcional para determinar si el mensaje debe ser 404 (no encontrado) o 403 (sin permisos)
}
